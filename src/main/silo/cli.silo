
package(silo.cli)
import(silo.core)

func(main(args : IPersistentVector) {
    println(version())
})

func(version String.format("Silo %s (Codename: %s)", silo.lang.Version.string, silo.lang.Version.codename))

