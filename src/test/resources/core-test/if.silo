
package(silo.test.core)

func(fib(a : int => int) {
    if(a == 0 {
        return(0)
    } else(a == 1) {
        return(1)
    } else {
        x : int = fib(a - 1)
        y : int = fib(a - 2)
        return(x + y)
    })

    return(-1)
})
